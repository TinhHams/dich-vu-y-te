<IfModule mod_rewrite.c>
  RewriteEngine On

# Bỏ qua file và thư mục thật (CSS, JS, hình ảnh, v.v.)

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tất cả các request còn lại đều chuyển về index.html

RewriteRule ^ index.html [L]
</IfModule>
